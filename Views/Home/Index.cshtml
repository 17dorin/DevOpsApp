@model List<Project> 
@{
    ViewData["Title"] = "Home Page";
}

@if(Model.Count != 0)
{
    foreach(Project p in Model)
    {
        <a>@p.ProjectName</a>
    }
}
else
{
    <h3>You currently do not have any projects. Please create a <a asp-controller="Projects" asp-action="Create">new project</a> or accept an invitation to one</h3>
}
